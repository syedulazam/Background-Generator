{
  "name": "background-generator",
  "version": "1.0.0",
  "description": "",
  "main": "backgroundGenerator.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx live-server",
    "build2": "npx browserify backgroundGenerator.js > bundle.js && npx live-server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.21"
  }
}
